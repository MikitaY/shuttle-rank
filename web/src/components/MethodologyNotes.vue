<script setup>
// Methodology of the active rating mode, shown inside the header's info panel.
defineProps({
  mode: { type: String, default: 'category' },
})

// Standalone methodology page in web/public — also works as a link to send someone.
const guideUrl = `${import.meta.env.BASE_URL}rating-guide.html`
</script>

<template>
  <div class="notes">
    <h4>Как считается</h4>

    <template v-if="mode === 'unified'">
      <p><b>Сквозной рейтинг</b> — одно число на игрока по всем матчам: одиночки, пары и микст
        вместе. Стартовый рейтинг зависит от категории, в которой игрок впервые вышел на корт
        (E 1100, D 1300, C 1500, B 1700, A 1900; сетка MASTER+ — как B), поэтому победа над
        игроком из более сильной категории стоит дороже с самого первого матча. Дебютировал
        сразу в двух категориях — старт по среднему.</p>
      <p><b>Изменение рейтинга</b> — как в Elo: ожидание считается от разницы рейтингов
        (шкала 400), шаг = K × (факт − ожидание). K падает с опытом: 40 в первых 10 матчах,
        32 до 30 матчей, дальше 24. Парные матчи и микст идут с весом 0,75 — половина
        результата принадлежит партнёру; ожидание в них считается от среднего рейтинга пары.
        Счёт в геймах не влияет, победы без игры (walkover) не учитываются.</p>
      <p><b>Категории</b> — полосы по 200 очков: E — до 1200, D — 1200–1399, C — 1400–1599,
        B — 1600–1799, A — 1800–1999, М+ — от 2000. Категория присваивается после 10 сыгранных
        матчей; до этого она показана серым — это категория дебюта, а не расчёт. Повышение —
        при достижении порога следующей категории; понижение — только если рейтинг упал
        на 50 очков ниже своего порога. «D → C» значит, что до следующей категории осталось
        не больше 60 очков. Нажми на категорию игрока — покажем, чего именно ему не хватает.</p>
      <p><b>Пары</b> — категории присваиваются только игрокам: у половины пар лиги меньше
        четырёх совместных матчей, свой рейтинг пары был бы вечно предварительным. Рейтинг
        пары в калькуляторе — среднее рейтингов игроков, то самое число, от которого движок
        считает ожидаемый результат парного матча.</p>
      <p>Клик по строке — история матчей с изменением рейтинга в каждом.</p>
    </template>

    <template v-else>
      <p><b>Elo</b> — все игроки стартуют с 1500, K-фактор 32. В парах ожидаемый результат
        считается от среднего Elo пары, дельта применяется каждому игроку. Победы без игры
        (walkover) в Elo не учитываются. Рейтинг считается по дисциплине (Одиночки/Пары/Микст),
        а выбор уровня (A/B/C/D/E/Masters) дополнительно сужает его до матчей именно этого
        уровня в этой дисциплине; в истории матчей дополнительно показан общий Elo игрока
        по всем встречам.</p>
      <p><b>Очки</b> — за победу: группа 10, четвертьфинал 15, полуфинал 20, финал 30 —
        умноженные на коэффициент уровня категории (E ×1.0, D ×1.25, C ×1.55, B ×1.9,
        A ×2.3, Masters ×2.3), плюс 5 очков за участие в каждом турнире (в общем зачёте).</p>
      <p><b>Уровень</b> — оценка по категориям, в которых игрок реально играет
        (SE = одиночки E, DC = пары C, XDB = микст B, MASTER+ и т.д.): средневзвешенный
        уровень сыгранных матчей с поправкой на процент побед (≥65% — сдвиг вверх ▲,
        ≤35% — вниз ▼). Это ориентир, а не официальный разряд.</p>
      <p>Клик по строке — история матчей игрока.</p>
    </template>

    <p>Данные собраны только из открытых страниц результатов турниров лиги.</p>

    <p class="guide-link">
      <a :href="guideUrl" target="_blank" rel="noopener">
        📄 Подробное объяснение сквозного рейтинга — схемы, формулы, разбор матчей
      </a>
    </p>
  </div>
</template>
